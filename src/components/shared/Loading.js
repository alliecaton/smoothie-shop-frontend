import ClipLoader from 'react-spinners/ClipLoader'

import React from 'react';

const Loading = () => {
    return (
        <div>
            <ClipLoader />
        </div>
    );
}

export default Loading;
